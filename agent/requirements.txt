# === DO Gradient ADK & LangGraph ===
gradient-adk>=0.2.11
gradient>=3.11.0
# NOTE: langchain-gradient is installed separately via build_command with --no-deps
# because it declares langchain-core<0.4.0 while we need langchain-core>=1.2.11.
# See .do/app.yaml and .github/workflows/ci.yml for the install pattern.
langchain-core>=1.2.11
langchain-openai>=1.1.9
langchain-anthropic>=1.0.0
langgraph>=1.0.0

# === 외부 LLM SDK (직접 호출 — ADR-A0) ===
openai>=1.60.0,<2.0.0
anthropic>=0.43.0,<1.0.0
google-genai>=1.0.0,<2.0.0

# === Web Framework ===
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.9.0
gunicorn>=23.0.0

# === DO SDK & Tools ===
pydo>=0.3.0
boto3>=1.35.0

# === YouTube & Search ===
yt-dlp>=2024.1.0
youtube-transcript-api>=0.6.3
google-api-python-client>=2.150.0

# === Database & Storage ===
httpx>=0.27.0
python-dotenv>=1.0.0
PyGithub>=2.0.0
asyncpg>=0.30.0
aiosqlite>=0.20.0

# === Build Validation (ADR-A4: Docker) ===
docker>=7.0.0
